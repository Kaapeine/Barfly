{
  "name": "barfly",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "start-profile": "web-ext run --keep-profile-changes --profile-create-if-missing --firefox-profile ~/.barfly-profile",
    "start": "web-ext run",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "web-ext": "^7.11.0"
  }
}